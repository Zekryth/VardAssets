{
  "name": "vard-assets-root",
  "private": true,
  "scripts": {
    "prebuild": "npm --prefix ./frontend-vue install --no-audit --no-fund && npm --prefix ./frontend-vue run build",
    "build": "vite build",
    "preview": "vite preview"
  },
  "engines": {
    "node": ">=18"
  }
}
