{
  "name": "vard-assets-root",
  "private": true,
  "scripts": {
    "prebuild": "npm --prefix ./frontend-vue install --no-audit --no-fund && node ./frontend-vue/node_modules/vite/bin/vite.js build",
    "build": "vite build",
    "preview": "vite preview"
  },
  "engines": {
    "node": ">=18"
  }
}
